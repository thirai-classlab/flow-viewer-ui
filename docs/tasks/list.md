# タスク台帳

status は 未着手 / 進行中 / 完了 の 3 種。

| # | status | タスク | 概要 | 依存先 | 詳細 |
|---|--------|-------|------|-------|------|
| 1 | 完了 | 初回 commit と public リポジトリ作成 | 現状（POC コピー + docs + ハーネス設定）を 1 commit。`gh repo create thirai-classlab/flow-viewer-ui --public`（承認要）→ push | — | 完了条件: `git log --oneline` に 1 件、`git remote -v` に origin、GitHub 上で閲覧できる → 済（2026-09-15、a9f23e4 + 6156202、https://github.com/thirai-classlab/flow-viewer-ui） |
| 2 | 未着手 | 品質ゲート（vitest + eslint） | `tests/` に `src/flow` の 32 ケース × 全ドリル経路の回帰（例外 0・ノード欠落 0）。eslint は typescript-eslint recommended。`npm test` / `npm run lint` を追加 | 1 | 完了条件: `npm run typecheck && npm run build && npm test && npm run lint` が exit 0 |
| 3 | 未着手 | lib / demo 分離と lib ビルド | `src/lib/`（publish 対象）/ `src/demo/` に移動。Vite lib モードで ES / CJS / `.d.ts` / `style.css` | 2 | 完了条件: [draft/flow-viewer-package.md](../draft/flow-viewer-package.md) §7 の 1 行目 |
| 4 | 未着手 | FlowViewer の公開 API と DocPanel 同梱 | mode / 設定 / theme はプロパティ、`ref` に `goUp()` / `drillInto(id)`、`onPathChange` / `onSelect`。`validateFlowDoc()`（入力: ノード 0 の doc は drilldown が `use-drill-effect.ts:232` で throw するので、検証で先に弾く）。DocPanel 読み取り専用。demo は公開 API だけで動く | 3 | 完了条件: 同 §7 の 2・3 行目 |
| 5 | 完了 | テーマ基盤（ライト既定 + ダーク） | CSS 変数トークン `--fv-*`、`data-theme` 切替（`theme` プロパティ化は #4）。2026-09-15 完了: 32 ケース × 2 テーマ = 64 枚で破綻 0・JS エラー 0、レビュー 2 巡で収束（未対応: LOW 1 件 = warn チップ背景を琥珀に揃えた意図的変更） | 3 | 完了条件: light / dark の 32 ケース目視、スクショが `docs/after/` にある → 済 |
| 6 | 完了 | 視覚言語の最適化（U1）+ アイコン | 決定（2026-09-15）: group 枠は破線→実線角丸 + 下辺バッジ + ホバーで枠色（glow 廃止）、記号・絵文字（📄 ▸ ⚙ ☾ ★ ⚡ 等 12 箇所）は Lucide の path を同梱して inline SVG に（依存追加なし、`<use>` は split の id 衝突で不可）、エッジ角丸 8、ドットグリッド間引き。サイズ定数は据え置き。group / task の判別性、exception / loopback の強調、線上ラベルの可読性。配色監査（2026-09-15）の入力: exception 赤と loopback 琥珀は色覚多様性下で近づくので線種以外の冗長化（矢尻・端点）を検討 / loopback 線と decision 枠が同系の琥珀で溶ける / 線上ラベル「不備あり」が枠に重なって欠ける / ライトの group 枠のアクセント青が強い / 📄 マークが絵文字依存 | 5 | 完了条件: 32 ケース目視、before / after スクショ、`npm test` green → 済（2026-09-16、レビュー 2 本（コード / a11y）Approve、MEDIUM 3 件対応済み。未対応 LOW: exception 始点の丸が塗り潰し（白抜きは view 側 override が要る）/ キャンバスのノード・バッジがキーボード到達不可（#4 の API で tabindex / role を検討）/ バッジの当たり判定 22px（サイズ定数据え置き）） |
| 7 | 未着手 | キャンバス内の情報の出し方（U2） | ヒント行と hint-bubble の二重解消、「▸ 中を見る」バッジ、ラベル | 6 | 完了条件: 同上 |
| 8 | 完了 | 手順書パネル（U3） | 決定（2026-09-15）: MD の見た目は mdv（~/markdownviewer2.0 の doc-preview.tsx `markdownStyles`）を `--fv-*` トークンに写像（本文 14px、余白拡大、表 zebra + 幅 100%、引用グレー帯、コード体裁）。レンダラは marked + DOMPurify のまま。領域は既定幅 45% + ドラッグリサイズ + 全画面（SidePanel の実装を移植、オーバーレイ維持で FlowCanvas の key に触れない）。完了 2026-09-16（571c105、レビュー 1 巡 Approve、MEDIUM 2 件は対応済み。未対応: LOW = 全画面解除の文言が SidePanel「全画面を解除」と DocPanel「戻す」で不一致 → #6 のアイコン化で解消） | 13 | 完了条件: 同上。`FlowCanvas` の `key` を見直したことを記録 |
| 9 | 未着手 | 操作の発見性（U4） | キャンバス上で「戻る」手段（`blank:dbclick` 無しの制約下） | 6 | 完了条件: 同上 |
| 10 | 未着手 | レスポンシブ（U5） | ホストの幅に追従 | 8 | 完了条件: 幅 800 / 1280 / 1920 で 32 ケース目視 |
| 11 | 未着手 | 匿名化実データの同梱 | ユーザー提供の実データ 1〜2 本を匿名化して `src/demo/data/` に `real:<id>` として追加 | 4 | 完了条件: `validateFlowDoc()` を通り、demo のデータ選択に出る。匿名化前は `*.local` のみ |
| 12 | 未着手 | npm publish 0.1.0 | `npm login` はユーザー（`! npm login`）。publish 前に承認 | 4, 5, 6 | 完了条件: `npm view @thirai-classlab/flow-viewer version` が `0.1.0` |
| 13 | 完了 | 自動レイアウト（dagre 化 + 折り返し廃止） | `@dagrejs/dagre` 3.1.1 で `arrange()` の中身を置換、pointsList / ラベル座標を LogicFlow へ渡す、tween 後に `updatePath()`、848 通りのヘッドレス sweep を `tests/` の vitest に | 1 | 完了条件: [draft/auto-layout.md](../draft/auto-layout.md) §7 の 4 行 → 済（2026-09-16、6f44a71。848 sweep green、レビュー 1 巡 Approve。未対応: MEDIUM 2 = 境界マーカーのエッジ経路が sweep 未カバー / `arrange()` 113 行 → #15 で drilldown を触るときに対応） |
| 14 | 完了 | サンプルデータのトップ階層に decision を追加 | 決定（2026-09-15）: 描画は変えず、`sample-data.ts` のトップに「審査結果」（OK→手配部門 / NG→謝絶 / 不備あり→受付部門）と「品質確認」（OK→完了 / 不通→手配部門）の decision を足し、中の分岐の出口をトップの decision に繋ぐ。中の decision は残す。実装（2026-09-15）: 「完了通知」は品質管理の中にあると 品質管理 ⇄ 品質確認 の往復になるためトップの task に出し、品質管理には「試験結果の記録」を追加。トップは 10 ノード（start / group 4 / decision 2 / task 1 / end 2） | 1 | 完了条件: drilldown トップにひし形 2 つが出て、32 ケースの回帰と `validateFlowDoc` 相当の整合（全 link の from/to が実在）を満たす → 済（33 ノード / 31 リンク / 未解決 0 / 重複キー 0） |
| 15 | 完了 | 読める縮尺の維持（fit の下限 + パン + 全体表示 + nested 自動抽象化） | 決定（2026-09-16）: 折り返し廃止で長いフローが縮尺 0.49（トップ 10 ノード）/ 0.2（nested）に落ちたため、fit の下限を 0.85 にして収まらないときは開始側（左 / 上）に寄せ、ホイール / ドラッグでパン・ズーム。「全体を表示」（fitAll）を `FlowCanvas` の ref に足し、⚙ から呼ぶ。nested は初期表示で下限を割る深さを自動で畳む（既存の collapsed に載せる。1 回だけ、ユーザー操作は上書きしない） | 13 | 完了条件: sample3 トップと nested が 1440×900 で縮尺 ≥ 0.85、開始ノードが見えている。ホイール / ドラッグで残りが見える。「全体を表示」で全体が収まる。848 sweep green → 済（2026-09-16、レビュー 1 巡 Approve。未対応 LOW: dev の StrictMode で nested 初期表示が一瞬全展開になる / nested を離れて再入場すると自動抽象化は再適用しない（仕様どおり）/ #13 の MEDIUM 2 件はここで対応済み） |
| 16 | 未着手 | 箱をまたぐ線の配線（dagre compound） | ユーザー指摘（2026-09-16）「線・文字・エッジが重なる」。#13 では同じ箱の直下どうしの線だけ dagre の経路を使い、箱をまたぐ線（nested の中身どうし / split 左ペインの入れ子に刺さる線 / コンテキスト層への crossing）は LogicFlow の自動経路（障害物回避なし）のまま。入れ子を 1 つの dagre compound グラフ（`compound: true` + `setParent`、cluster のヘッダ帯は `clusterLabelPos`）で解き、全エッジに pointsList / ラベル座標を渡す。measure() の再帰箱詰めは compound に置き換える（drilldown の levelOnly は現行のまま）。詳細: [draft/compound-routing.md](../draft/compound-routing.md)（2026-09-16 承認） | 13 | 完了条件: 848 sweep（nested 全展開・split 入れ子含む）で 例外 0 / 重なり 0 / 線がノードを貫通 0 / ラベル同士の重なり 0 を assert。sample3 の split（審査部門 › 与信審査）と nested のスクショで NG・不備あり・重複の線が箱の縁を這わない |
| 17 | 完了 | split 左ペインの decision をひし形に | ユーザー指摘（2026-09-16）。左ペインは `toSplitUpperNode`（nodes.ts）が kind を無視して rect 固定。decision だけ diamond（SPLIT.nodeSize と同じ外形寸法）にする。★ / ⚡ の前置ラベルはそのまま | 6 | 完了条件: sample3 の split で左ペインの「審査結果」「品質確認」がひし形、クリックで移動できる、848 sweep green → 済（2026-09-16、#6 と同じ commit） |

